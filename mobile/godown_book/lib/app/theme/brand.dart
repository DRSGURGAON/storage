import 'package:flutter/material.dart';

/// The StorageBill Pro palette, as the brand sheet states it, plus the
/// handful of tints the screens are built from. One place, so a colour
/// is never invented twice.
class Brand {
  const Brand._();

  static const navy = Color(0xFF0D2B49);
  static const navyDeep = Color(0xFF081C33);
  static const navySoft = Color(0xFF16375C);
  static const navyLine = Color(0xFF1E4470);

  static const green = Color(0xFF14D1A0);
  static const greenInk = Color(0xFF06261E);

  /// The page behind everything - warm, not clinical white.
  static const paper = Color(0xFFF6F3EE);
  static const card = Colors.white;
  static const line = Color(0xFFE8E3DB);
  static const chip = Color(0xFFF1EEE8);

  static const ink = Color(0xFF17202B);
  static const inkMuted = Color(0xFF7A7F87);
  static const inkOnNavyMuted = Color(0xFF9FB3C8);

  static const amber = Color(0xFFF5A524);
  static const amberSoft = Color(0xFFFFF4E0);
  static const amberInk = Color(0xFF7A4700);

  static const coral = Color(0xFFFF6B6B);
  static const coralSoft = Color(0xFFFDECEC);
  static const coralInk = Color(0xFF8E2626);
  static const coralDeep = Color(0xFFB23B3B);

  static const mintSoft = Color(0xFFE6F8F2);
  static const mintInk = Color(0xFF0B5C45);

  static const skySoft = Color(0xFFE9F0FA);
  static const skyInk = Color(0xFF2F62A8);
}
