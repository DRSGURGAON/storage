gradle-wrapper.jar
/.gradle
/captures/
/gradlew
/gradlew.bat
/local.properties
GeneratedPluginRegistrant.java
.cxx/

# Remember to never publicly share your keystore.
# See https://flutter.dev/to/reference-keystore
key.properties
**/*.keystore
**/*.jks

# Never commit signing or Firebase credentials
key.properties
**/*.keystore
**/*.jks
app/google-services.json

# The one keystore that IS shared: it signs test builds from CI so every
# build carries the same signature and installs over the last one as an
# update. It is not the Play release key (that stays in secrets).
!dev-signing.jks
