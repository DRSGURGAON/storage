{
  "name": "storagebill-firestore-rules-tests",
  "private": true,
  "description": "Security-rules tests for firestore.rules, run against the Firestore emulator.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "cd .. && firestore_tests/node_modules/.bin/firebase emulators:exec --only firestore --project demo-storagebill \"node --test firestore_tests/rules.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.2",
    "firebase": "^12.0.0",
    "firebase-tools": "^15.0.0"
  }
}
